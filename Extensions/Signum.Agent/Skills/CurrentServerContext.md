
### Current Server Context

The tool `GetCurrentServerContext` returns contextual information from the server like:

- Current date/time, UTC date and time zone:
- Current user and role
- Current culture
- Current application URL
