@using Signum.Entities.Processes

@using (var e = Html.TypeContext<PackageLineDN>())
{
    e.ReadOnly = true;
    
    @Html.EntityLine(e, f => f.Package)
    @Html.EntityLine(e, f => f.Target)
    @Html.EntityLine(e, f => f.Result)
    @Html.ValueLine(e, f => f.FinishTime)

    @Html.SearchControl(new FindOptions(typeof(ProcessExceptionLineDN), "Line", e.Value), new Context(e, "Exceptions"))
} 