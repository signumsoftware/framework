@using Signum.Web.Auth

@using (var e = Html.TypeContext<SetPasswordModel>())
{ 
    @Html.HiddenRuntimeInfo(e, sp => sp.User);
    @Html.ValueLine(e, sp => sp.Password);
    @Html.ValueLine(e, sp => sp.RepeatPassword);
}
