@using Signum.Entities.Mailing

@using (var sc = Html.TypeContext<ClientCertificationFileDN>())
{
    using (Html.FieldInline())
    {
    @Html.ValueLine(sc, s => s.CertFileType)
    @Html.ValueLine(sc, s => s.FullFilePath)
    }
}
