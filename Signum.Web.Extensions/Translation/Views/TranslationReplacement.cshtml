@using Signum.Entities.Translation
@using (var tc = Html.TypeContext<TranslationReplacementEntity>())
{
    @Html.EntityLine(tc, t => t.CultureInfo)
    @Html.ValueLine(tc, t => t.WrongTranslation)
    @Html.ValueLine(tc, t => t.RightTranslation)
}