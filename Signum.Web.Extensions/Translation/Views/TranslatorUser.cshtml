@using Signum.Entities.Translation
@using (var tc = Html.TypeContext<TranslatorUserDN>())
{
    @Html.EntityLine(tc, t => t.User)
    @Html.EntityRepeater(tc, t => t.Cultures, er => er.Move = true)
}