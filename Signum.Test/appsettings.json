{
  // Base configuration file
  // Connection strings are in appsettings.SqlServer.json and appsettings.Postgres.json
  // Set TEST_DATABASE environment variable to choose: SqlServer (default) or Postgres
}
