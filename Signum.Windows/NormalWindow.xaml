<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:m="clr-namespace:Signum.Windows"
	x:Class="Signum.Windows.NormalWindow"
	x:Name="Window"
	SizeToContent="WidthAndHeight"
	ResizeMode="CanResizeWithGrip" 
    m:Monitors.AdjustToMonitor="true"
	WindowStartupLocation="CenterScreen" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding ToStringMethod}">
    <DockPanel>
        <m:ButtonBar x:Name="buttonBar" DockPanel.Dock="Top" OkClick="Ok_Click" SaveClick="Save_Click" ReloadClick="Reload_Click" ReloadVisible="true" MainControl="{Binding Path=MainControl, ElementName=Window}"/>
        <m:WidgetPanel x:Name="widgetPanel" DockPanel.Dock="Left" MainControl="{Binding Path=MainControl, ElementName=Window}" ExpandedCollapsed="widgetPanel_ExpandedCollapsed" />
        <m:ErrorSummary ValidationTarget="{Binding Path=MainControl, ElementName=Window}" DockPanel.Dock="Bottom"/>
        <m:EntityTitle DockPanel.Dock="Top"/>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Focusable="False">
            <ContentControl Content="{Binding Path=MainControl, ElementName=Window}" Focusable="False"
                        VerticalContentAlignment="Stretch" x:Name="contentControl" 
                        HorizontalContentAlignment="Stretch" MinWidth="40" MinHeight="40" Margin="5"/>
        </ScrollViewer>
    </DockPanel>
</Window>